# Sorry-solving cheat sheet (Tao Analysis in Lean)

Working notes for solving `sorry`s in `analysis/Analysis/*.lean`.

## Workflow
- Build a single file fast: `lake env lean Analysis/Section_X_Y.lean` (no `lake build` needed for a quick check). Exit 0 + no `declaration uses 'sorry'` warning = clean.
- Find easy targets: `grep -rc sorry --include=*.lean Analysis | grep -v :0 | sort -t: -k2 -n`.
- Commit after each net decrease in sorrys: `git commit -m "solve N sorrys in Section_X_Y.lean"`. End message with the Co-Authored-By trailer.
- `-Dwarn.sorry=false` is set for `lake build`, but `lake env lean <file>` DOES print sorry warnings — use that to confirm.

## Tips learned
- **Instance fields**: when filling an `instance ... where` field, `#print <ClassName>` to get the EXACT field signature and binder order. The `intro` order must match the field's argument order, NOT the standalone lemma's. Example: `MulPosMono.mul_le_mul_of_nonneg_right : ∀ ⦃c⦄, 0 ≤ c → ∀ ⦃a b⦄, a ≤ b → a*c ≤ b*c` → `intro c _ a b hab`.
- The Chapter 2 `Nat` defines `≤` as `∃ d, b = a + d`; `obtain ⟨d, hd⟩ := hab` then `rw [hd, add_mul/mul_add]`.
- Mirror a sibling proof in the same file when one direction (left/right) is already done.
- **Restricted-filter limits** `atTop ⊓ 𝓟 (range f)`: rewrite with `← Filter.map_comap` (gives `map f (comap f atTop) = atTop ⊓ 𝓟 (range f)`), then `Nat.comap_cast_atTop` (`comap cast atTop = atTop`) collapses it to `map cast atTop`, then `Filter.tendsto_map'_iff` turns `Tendsto g (map f l)` into `Tendsto (g∘f) l`. Great for "sequence vs function-on-ℝ-restricted-to-ℕ" equivalences.
- `tendsto_inv_atTop_zero : Tendsto (·⁻¹) atTop (nhds 0)`; combine with `Filter.Tendsto.mono_left _ inf_le_left` for restricted filters; `simpa [one_div]` to match `1/x`.
- **IVT / continuous image of Icc**: `ContinuousOn.image_Icc (hab : a ≤ b) : f '' Icc a b = Icc (sInf ..) (sSup ..)`. `intermediate_value_Icc (hab) (hf) : Icc (f a) (f b) ⊆ f '' Icc a b`; primed version `intermediate_value_Icc'` uses `Icc (f b) (f a)` (decreasing). Fixed-point: set `g = f x - x`, apply `intermediate_value_Icc'` to get `0 ∈ g '' Icc`.
- **Monotone deriv ≥ 0**: `hderiv.hasDerivWithinAt` then `rw [hasDerivWithinAt_iff_tendsto_slope]`; bound `slope f x₀ y ≥ 0` via `slope_def_field` + `div_nonneg`/`div_nonneg_iff` (no `div_nonneg_of_nonpos_of_nonpos` lemma — use `div_nonneg_iff` and pick `Or.inr`); finish with `ge_of_tendsto` (need `NeBot` from the `ClusterPt` hyp: `haveI : (nhdsWithin x₀ (X\{x₀})).NeBot := hx₀`).
- **derivWithin = deriv** on interior of Icc: `rw [← derivWithin_of_mem_nhds (Icc_mem_nhds hx.1 hx.2)]` (after `rw [interior_Icc]`). Or `hf.derivWithin huniq` / `hderiv.hasDerivWithinAt.derivWithin huniq` where `huniq : UniqueDiffWithinAt`.
- **StrictMonoOn from positive deriv**: `strictMonoOn_of_deriv_pos (convex_Icc a b) hf.continuousOn`; the `hf'` arg needs `deriv` (global) on `interior D` — convert from `derivWithin`.
- **UniqueDiffWithinAt for a set union**: `(uniqueDiffOn_Icc (by norm_num) x hmem).mono Set.subset_union_left/right`. `UniqueDiffWithinAt.mono` is monotone in the set (s ⊆ t).
- **Counterexample to "positive derivWithin ⟹ strict mono"** needs a DISCONNECTED domain (the theorem holds on intervals). Use `f = x³-3x` on `[-2,-3/2] ∪ [3/2,2]`: globally differentiable (poly), `f'=3x²-3>0` since `|x|≥3/2`, but `f(-3/2)=9/8 > f(3/2)=-9/8`. Build deriv via `HasDerivAt`: `simpa using hasDerivAt_pow 3 x`, `(hasDerivAt_id x).const_mul 3`, `.sub`.
- `Set.mem_image_of_mem` (NOT bare `mem_image_of_mem`). `Set.mem_Icc` to unpack `x ∈ Icc a b` into `a ≤ x ∧ x ≤ b`.
- **Beta reduction is finicky**: after `refine ⟨fun x => …, …⟩`, sometimes the goal/hyp is auto-beta-reduced and `dsimp only` then ERRORS with "no progress"; other times it's NOT reduced and `split_ifs`/`rw` fails to match the lambda. Don't blanket-add `dsimp only`. For a goal use `show <reduced form>` to force it; for hyps from `h x hx`, try WITHOUT dsimp first (often already reduced) and only add `dsimp only at h` if a later `rw` fails to match. `nlinarith` ring-normalizes so it tolerates unreduced lambdas.
- **`IsMinOn`/`IsMaxOn`**: `rw [isMinOn_iff]`/`isMaxOn_iff` → `∀ x ∈ s, f a ≤ f x` / `∀ x ∈ s, f x ≤ f a`. To refute existence of a max on an OPEN interval, pick a witness strictly past the candidate toward the (excluded) endpoint, e.g. `3/2 + (|x₀-3/2|+1/2)/2`, and `nlinarith` with `sq_abs`, `abs_nonneg`, and an explicit `mul_pos` of the two positive factors.
- `max M 0 + 1` is the canonical "bigger than M and ≥ 1" gadget for unboundedness proofs; `le_max_left`/`le_max_right` give the two bounds (mind which!). `1/(max M 0+1)` lands in `(0,1]`.
- **Chapter 4 rationals order theory**: `formalDiv_eq_div a hb : a//b = (↑a)/(↑b)` (helper — proves via `coe_Int_eq, div_eq, inv_eq b one_ne_zero, mul_eq, eq; ring`; note inv_eq's denominator is `1` so pass `one_ne_zero` not `hb`) bridges `//` to field division and is the key to `isPos`/`isNeg` proofs. `trichotomous`: `rcases lt_trichotomy a 0`/`b 0`, supply `±a,±b` witnesses (make numerators EXPLICIT in `formalDiv_eq_div`, the implicit `b` is inferred from the nonzero arg). `not_pos_and_neg`: cross-multiply via `div_eq_div_iff hbne hdne` then `intCast_mul` (FORWARD: `↑a*↑b → ↑(a*b)`) + `coe_Int_inj.eq_iff` + `nlinarith [mul_pos …]`. `intCast_ne_zero ha : (↑a:Rat)≠0`. Order trichotomy lemmas reduce to `trichotomous (x-y)` + `gt_iff`/`lt_iff`/`sub_eq_zero`/`sub_self`; `antisymm : x<y ↔ y>x` is `Iff.rfl`. `IsStrictOrderedRing` field sigs: `add_le_add_left a b hab c` (goal `a+c ≤ b+c`!), `mul_lt_mul_of_pos_left a ha b c hbc` (`ha:0<a`, goal `a*b<a*c`), `le_of_add_le_add_left a b c h`. KEY for `formalDiv`'s `if h:b≠0` guard: prove helper `formalDiv_mk a hb : a // b = Quotient.mk PreRat.instSetoid ⟨a,b,hb⟩` by `show Quotient.mk PreRat.instSetoid _ = _; rw [dif_pos hb]` (NOT `simp [formalDiv, dif_pos]` — leaves the dite). Then `equivRat_apply a hb : equivRat (a//b) = (a:ℚ)/b` is `rw [formalDiv_mk a hb]; rfl`. For equiv `map_add'`/`map_mul'` (RingEquiv fields): prepend `show equivRat (a//b + c//d) = equivRat (a//b) + equivRat (c//d)` to convert the toFun coe into `equivRat` applications, then `rw [add_eq…, equivRat_apply…, div_add_div…]; push_cast; ring`. `left_inv`: `rw [formalDiv_mk a hb]; exact (coe_Rat_eq a hb).trans (formalDiv_mk a hb)` (defeq `exact`, no `show` — show's cast elaboration double-coerces). `right_inv`: `show Quotient.lift _ _ ((q.num:ℤ)//(q.den:ℤ)) = q; rw [formalDiv_mk q.num hd]; exact_mod_cast Rat.num_div_den q`. `ratCast_inj` via `Rat.eq` + `div_eq_div_iff` + `Rat.num_div_den`. **Order-iso `equivRat_order.map_rel_iff'`** (SOLVED): build sign-preservation lemmas using `equivRat_ring` (RingEquiv → has `map_div₀`/`map_neg`/`map_sub`/`map_zero`): `equivRat_intCast p : equivRat ↑p = ↑p`; `isPos_equivRat_pos {z} (h:z.isPos) : 0 < equivRat z` (`obtain ⟨p,q,…,rfl⟩; show 0 < equivRat_ring (↑p/↑q); rw [map_div₀, equivRat_intCast, equivRat_intCast]; positivity`); `isPos_iff_equivRat_pos` (backward via `trichotomous`); `isNeg_iff_neg_isPos`; `equivRat_lt x y : x<y ↔ equivRat x < equivRat y` (`rw [lt_iff, isNeg_iff_neg_isPos, isPos_iff_equivRat_pos, <map_sub via neg_sub>]; constructor <;> intro <;> linarith`); then `map_rel_iff'` is `intro a b; show equivRat a ≤ equivRat b ↔ a ≤ b; rw [← not_lt, ← not_lt, equivRat_lt]`. **`decidableRel`** (SOLVED — Section_4_2 fully closed): since it precedes `equivRat`, build the characterization from primitives: `formalDiv_isPos {x y} (hy) : (x//y).isPos ↔ 0 < x*y` (forward: `rw [← formalDiv_eq_div, eq]` to get `x*q = p*y`, then `have key : (x*y)*q = p*(y*y)` + `nlinarith [key, mul_pos hp (mul_self_pos.mpr hy), hq]`; backward: `rcases lt_trichotomy y 0`, witnesses `±x,±y`); `formalDiv_isNeg` (via `(x//y).isNeg ↔ ((-x)//y).isPos` using `neg_eq`); `formalDiv_eq_zero`; then `formalDiv_le_iff a c hb hd : a//b ≤ c//d ↔ (a*d-c*b)*(b*d) ≤ 0` (`rw [le_iff, lt_iff, hsub, formalDiv_isNeg, eq]` where `hsub : a//b - c//d = (a*d-c*b)//(b*d)`). decidableRel: `rw [← formalDiv_mk, ← formalDiv_mk, formalDiv_le_iff]`, case on `(0).decLe (b*d)` sign, discharge each with `nlinarith [hbd', h]` (derive `0 < b*d` via `lt_of_le_of_ne`).
- **Section_7_2 (custom `Series` laws)**: `convergesTo s L := Tendsto s.partial atTop (𝓝 L)`; `sum := if h:converges then h.choose else 0`. Zero test `decay_of_converges`: `seq n = partial n - partial (n-1)` (from `partial_succ` + `sub_add_cancel`), so `(hL.sub (hL.comp (tendsto_atTop_add_const_right atTop (-1) Filter.tendsto_id))).congr'` with `filter_upwards [eventually_ge_atTop s.m]`. Divergence examples: `diverges_of_nodecay` + `Metric.tendsto_atTop` + index `max N 0` + `eval_coe`/`abs_pow`. Linearity: prove `(s+t).partial = fun N => s.partial N + t.partial N` by `funext N; show ∑ Icc (min s.m t.m) N (s.seq+t.seq) = …; rw [Finset.sum_add_distrib]; congr 1 <;> (symm; apply Finset.sum_subset (Finset.Icc_subset_Icc_left (min_le_left/right _ _)); … exact s.vanish n (by omega))` (the extra terms below each `.m` vanish), then `show Filter.Tendsto (s+t).partial …; rw [hpe]; exact Filter.Tendsto.add hs ht` (use `Filter.Tendsto.add`/`.const_mul`/`.sub` EXPLICITLY — `hs.add` resolves to the recursive theorem!). `convergesTo.sum_eq (h) : s.sum = L` via `unfold sum; rw [dif_pos ⟨L,h⟩]; tendsto_nhds_unique (·.choose_spec) h`, then `converges`+`sum` versions follow from `⟨⟨L+M, hL.add hM⟩, by rw [….sum_eq, ….sum_eq]⟩`.
- **Finite ℤ-partial-sum formulas (e.g. `example_7_2_4a`: `partial N = 1 - 2^(-N)`)**: `induction N, hN using Int.le_induction`. Base: `show s.partial <m> = _; unfold Series.partial; rw [Finset.Icc_self, Finset.sum_singleton, Series.eval_mk' _ (le_refl _)]; norm_num`. Succ: `have hm : s.m = <m> := rfl; rw [Series.partial_succ s (by rw [hm]; omega), ih, Series.eval_mk' _ (by omega)]` then close the zpow arithmetic. zpow step: `(2:ℝ)^(-(N+1)) = 2^(-N)*2⁻¹` via `rw [show -(N+1) = -N + (-1:ℤ) by ring, zpow_add₀ (by norm_num : (2:ℝ) ≠ 0)]; norm_num`, then `rw [e]; ring`.
- **`Tendsto (fun N:ℤ => r^(-N)) atTop (𝓝 0)` (0<r<1-ish, here r=2)**: `(tendsto_pow_atTop_nhds_zero_of_lt_one h0 h1).comp htn` with `htn : Tendsto Int.toNat atTop atTop`, then `.congr'` + `filter_upwards [eventually_ge_atTop 0] with N hN`; bridge `(1/2)^N.toNat = 2^(-N)` via `rw [one_div, inv_pow, zpow_neg, ← zpow_natCast, Int.toNat_of_nonneg hN]`. Convergent value via `Tendsto.congr'` against the closed-form `partial`, then `(tendsto_const_nhds).sub hg`. `sum = L` from `convergesTo` via `sum_of_converges`. For the matching divergence (`2^n`), `diverges_of_nodecay` + `one_le_zpow₀ (by norm_num) (by omega)` at index `max N <m>`. NOTE: zero-test lemmas (`decay_of_converges`/`diverges_of_nodecay`) are defined mid-file — proofs that need them must come AFTER, or be relocated.
- **Index-shift & telescoping (`Series.shift`, `Series.telescope`)**: don't reindex the Finset (no `map_add_right_Icc` in Mathlib). Instead prove the partial-sum identity by `Int.le_induction` from the lowest relevant index, both sides via `partial_succ`. shift: `∀ M ≥ s.m+L-1, t.partial M = s.partial (M-L)` (base: both Icc empty → `Finset.Icc_eq_empty (by omega)`; succ: `partial_succ t`, `ih`, `show M+1-L=(M-L)+1 by ring`, `partial_succ s`, then `eval_mk'` for `t.seq`), then `h.comp hg` with `hg : Tendsto (·-L) atTop atTop` (`simpa using tendsto_atTop_add_const_right atTop (-L) tendsto_id`) and `.congr'`. telescope: partial = `a 0 - a(N.toNat+1)` → `(tendsto_const_nhds).sub (ha.comp hidx)`. **Motive-not-type-correct gotcha**: `rw [show N+1 = ↑(N+1).toNat …]` loops (RHS contains N+1). Fix: pull `eval_coe`/`eval_mk'` into a separate `have` (`Series.eval_coe g 0` as a TERM, or `have hk : (N+1:ℤ) = ((N.toNat+1:ℕ):ℤ) := by omega; rw [hk, eval_coe]`) so the rewrite target is isolated.
- **Series tails (`converges_from`/`sum_from`, Section_7_2)**: factor out a `partial_from` helper: `∀ N ≥ s.m+k-1, (s.from (s.m+k)).partial N = s.partial N - ∑ n ∈ Ico s.m (s.m+k), s.seq n`, by `Int.le_induction` (base: from-partial is empty Icc → 0, and `Finset.Icc_sub_one_right_eq_Ico : Icc a (b-1) = Ico a b` converts the leftover; succ: both `partial_succ` + `eval_mk'` for `from.seq`, then `ring`). Convergence iff: `(hL.sub_const _).congr'` / `(hL.add_const _).congr'` against `partial_from` over `eventually_ge_atTop (s.m+k-1)`. `from.m = max s.m (s.m+k)` proven by `show max … = …; omega` (omega knows max/min on ℤ). Note `.sum_eq` (= `convergesTo.sum_eq`) and `Filter.Tendsto.sub_const`/`add_const` reachable via dot-notation on `convergesTo` (reducible abbrev unfolds to `Tendsto`).
- **Absolute convergence & comparison (Section_7_2/7_3, high-value chain)**: `partial_split {a≤b, s.m≤a} : s.partial b = s.partial a + ∑ k ∈ Icc (a+1) b, s.seq k` (induction; succ uses `← Finset.insert_Icc_right_eq_Icc_add_one (a+1≤b+1)` + `Finset.sum_insert`). `converges_of_absConverges`: bound `|s.partial b - s.partial a| ≤ s.abs.partial b - s.abs.partial a` via `partial_split` (both) + `Finset.abs_sum_le_sum_abs`, then `CauchySeq s.partial` from `Metric.cauchySeq_iff` (use `max N s.m` as the threshold, `rcases le_total a b` for symmetry) → `cauchySeq_tendsto_of_complete`. Key Mathlib: `hA.cauchySeq` (Tendsto→CauchySeq), `hs.abs` (Tendsto of `|f|`). `abs_le`: `le_of_tendsto_of_tendsto' hs.abs habs hpt` with `hpt N : |s.partial N| ≤ s.abs.partial N`. Comparison test `converges_of_le`: from `hcomp : ∀ n≥s.m, |s.seq n| ≤ t.seq n` derive `t.nonneg`/`s.abs.nonneg` (case `le_or_gt s.m n`, vanish below `.m`), `s.abs.partial N ≤ t.partial N ≤ t.sum`, then `(converges_of_nonneg_iff habsnn).mpr ⟨t.sum, hbound⟩`, `abs_le`, `sum_of_nonneg_lt`. `s.abs.seq n = |s.seq n|` for `n≥s.m` via `Series.eval_mk' _ hn`. NOTE: `le_or_lt` renamed → `le_or_gt`.
- **Cauchy criterion (`converges_iff_tail_decay`)**: helper `hsum : ∀ p, s.m≤p → ∀ q, p-1≤q → ∑ n ∈ Icc p q, s.seq n = s.partial q - s.partial (p-1)` (induction on q from p-1; base `Icc_eq_empty`+`sub_self`). Forward: `(convergesTo_sum hconv).cauchySeq` + `Metric.cauchySeq_iff`, threshold `max (N0+1) s.m`, `rcases le_or_gt p q` (empty Icc when p>q), `abs_sub_comm` + `← Real.dist_eq`. Backward: build `CauchySeq` from the tail bound at `ε/2` (criterion gives `≤`, cauchySeq_iff wants `<` → `lt_of_le_of_lt _ (by linarith)`), `rcases le_total a b`, convert `s.partial a - s.partial b` to `∑ Icc (b+1) a` via `hsum`/`partial_split` (simplify `(a+1)-1 = a` with a separate `have … := by omega`), then `cauchySeq_tendsto_of_complete`.
- **Alternating series test (`converges_of_alternating` why1–why5)**: `why1/why2` induct on `k:ℕ`, rewriting the index `N+2*↑(k+1)` to `(N+2*↑k)+2` via `congr 1; push_cast; ring`, stepping with `claim3`/`claim2` (even/odd S(N+2) bounds) and the IH; `even`/`odd` facts via `h'.add (even_two_mul _)` and `.add_one`. `why3` is one `claim0` + `Odd.neg_one_zpow` + `nlinarith [ha _]`. `why4` (both bounds for any `n≥N`): `obtain ⟨j,hj⟩ : ∃ j:ℕ, n = N + j := ⟨(n-N).toNat, by omega⟩`, `rcases Nat.even_or_odd j`, rewrite `n` and read off `claim4`. `why5` (Cauchy bound from `a→0`): **the subtype `{n//n≥m}` has `SemilatticeSup` but NOT `Nonempty`** — supply `haveI : Nonempty {n:ℤ // n ≥ m} := ⟨⟨m, le_refl m⟩⟩` before `Metric.tendsto_atTop`; pick even threshold `2 * max (max N0.val m) 0`, subtype order goal closes via `show N0.val ≤ …; omega`. NOTE: inside `Series.*` proofs `abs_le` resolves to `Series.abs_le` — use `_root_.abs_le`.
- **Conditional convergence examples (`example_7_2_13a/b/c`, alternating harmonic)**: 13a (converges): `set a := fun n:{k:ℤ//k≥1} => 1/((n:ℤ):ℝ)`, prove `ha`/`Antitone a` (`one_div_le_one_div_of_le`), and `Tendsto a (𝓝 0)` by composing `Filter.tendsto_comp_val_Ici_atTop (a:=1) (f:=id) |>.mpr tendsto_id : Tendsto Subtype.val atTop atTop` with `tendsto_inv_atTop_zero.comp tendsto_intCast_atTop_atTop`; then `show example_7_2_13 = mk' (…); congr 1; funext; rw [div_eq_mul_one_div]` and apply `converges_of_alternating`. 13b (¬absConverges, harmonic diverges): **`nonneg`/`converges_of_nonneg_iff` live in Section_7_3, NOT importable here** — instead `import Mathlib.Analysis.PSeries`, bridge `abs.partial (k:ℤ) = ∑ i ∈ range k, (1/(i+1):ℝ)` by induction (the seq is zpow → use `abs_zpow`/`one_zpow`, not `abs_pow`), then a convergent `abs.partial` would `Tendsto` both to `𝓝 L` (via `hL.comp tendsto_natCast_atTop_atTop`) and `atTop` (via `Real.tendsto_sum_range_one_div_nat_succ_atTop` + `Filter.Tendsto.congr`) — `not_tendsto_atTop_of_tendsto_nhds`. 13c = `⟨13a, 13b⟩`. Decidability of a diverging series: `apply isFalse; intro hconv; decay_of_converges` + `Metric.tendsto_atTop` at index `max N 1`, `abs_zpow` → contradiction.
- **`nonneg_sum_zero` (Section_7_3, nonneg series sums to 0 ⟺ all terms 0)**: forward — each `partial ↑n = 0` by `le_antisymm` of `partial_le_sum_of_nonneg` (rewrite `hsum` INTO the hypothesis, not `← hsum` into the goal — the latter hits the `else 0` inside the coercion → "motive not type correct"), then `partial_succ` + `eval_coe` gives `a n = partial ↑n - partial(↑n-1) = 0`. Backward — `partial N = 0` for all N (`Finset.sum_eq_zero`, `rw [hm]` to expose `Icc 0 N`), then `rw [show partial = fun _ => 0 from funext hpe]` (simp won't rewrite an unapplied function with a pointwise eq) + `tendsto_const_nhds` + `sum_of_converges`.
- **Cauchy ⟹ bounded (`isBounded_of_isCauchy`, Section_5_1 ℤ-indexed `Sequence`)**: get 1-eventually-steady (`h 1 one_pos` → `⟨N, hN, hsteady⟩`), `rw [Rat.steady_def]`; `(a.from N).n₀ = N` by `show max a.n₀ N = N; omega`. Tail bound `∀ n≥N, |a n| ≤ |a N|+1` via `from_eval` + `abs_add_le` (NOT `abs_add` — renamed). Head bound: `IsBounded.finite (fun i:Fin (N-a.n₀).toNat => a (a.n₀ + (i:ℕ)))`; reconstruct index with `rw [show a.n₀ + ((n-a.n₀).toNat:ℤ) = n by omega]`. Combine with `max M0 (|a N|+1)`; for `n < a.n₀` use `a.vanish`. NOTE inequality-in-abs equalities: `calc |a n| = |a N + (a n - a N)| := by congr 1; ring` (ring can't go under `|·|`).
- **Chapter 6 `Sequence` (ℤ-indexed, `.m` start) TendsTo**: `Sequence.tendsTo_iff : a.TendsTo L ↔ ∀ε>0 ∃N ∀n≥N, |a n - L| ≤ ε` proved by unfolding `EventuallyClose`/`CloseSeq` (`obtain ⟨N, hNm, hclose⟩`), witness `max a.m N`, bridge via `Sequence.from_eval a (hn : n ≥ m₁)` + `Real.Close` + `Real.dist_eq`; backward needs `have hm : (a.from (max a.m N)).m = max a.m N := by show max a.m (max a.m N)=…; omega` then `rw [hm] at hn`. **Subsequences** (`convergent_iff_subseq`): forward uses `tendsTo_iff` on both, witness `max N 0`, `StrictMono.id_le` (`n ≤ f n`), index `f n.toNat`; backward is `h a (subseq_self a)`. Subsequence examples: `refine ⟨fun n => 2*n(+1), ?_, fun n => ?_⟩`, `dsimp only` to beta-reduce before `rw [if_pos (even_two_mul n)]`/`if_neg`, then `congr 2; omega` for the `ℤ`-division exponent. GOTCHA: `omega` sometimes fails to link `↑n.toNat` to `n` after a goal `rw` — use `Int.toNat_of_nonneg` + `le_trans` explicitly.
- **Chapter 6 from-commutation & archimedean limits**: `is_eventuallySteady_of_rat` needs `(↑a).from N = ↑(a.from N)` — prove by `ext n` (Sequence is `@[ext]`; goals come **m-first, seq-second**), seq case `by_cases n ≥ N` using `from_eval`/`coe_sequence_eval`/`vanish`+`norm_num`; then `is_steady_of_rat` + `rw [hcomm]`. `seq_6_1_6` limits: helper `heval n (hn:n≥0) : seq n = 1 - 10^(-n-1)` via `rw [seq_def]; simp only [instCoeFun, ofNatFun, ge_iff_le]; rw [if_pos, Int.toNat_of_nonneg hn]`. Fixed-N (`EventuallyClose`): `from_eval` + `zpow_le_zpow_right₀ (by norm_num) (by omega)`. `TendsTo` (archimedean): `exists_nat_gt (1/ε)`, threshold `max K 1`, bound `(n:ℝ)+1 ≤ 10^(n+1)` via `one_add_mul_le_pow (a:=9)` on `(n+1).toNat` (cast with `Int.toNat_of_nonneg` + `exact_mod_cast`; `norm_num at key` to fold `1+9→10`), then `div_lt_iff₀`/`div_le_iff₀` + `nlinarith [hK, mul_le_mul_of_nonneg_right …]`.
- **Chapter 6 max/min & from/shift limit laws**: `tendsTo_max`/`min` use `abs_max_sub_max_le_max`/`abs_min_sub_min_le_max` (`|max x y - max u v| ≤ max |x-u| |y-v|`) — take SAME ε for both, `refine ⟨max Na Nb,…⟩`, `rw [Sequence.max_eval/min_eval]`, `le_trans (abs_…) (max_le h1 h2)`. `mono_if`: `strictMono_nat_of_lt_succ (fun k => ha k) hnm`. `tendsTo_of_from` (`a.TendsTo c ↔ (a.from m).TendsTo c`): both via `tendsTo_iff`, threshold `max N m`, `Sequence.from_eval`. `tendsTo_of_shift` (`mk' a.m (fun n => a (n+k))`): forward threshold `max N a.m` + `eval_mk` + index `n+k`; backward threshold `max (N+k) (a.m+k)`, index `n-k`, `rw [show (n-k)+k = n by ring]`.
- **Chapter 5 ℝ order (Section_5_4, `IsPos x := ∃ a, BoundedAwayPos a ∧ Cauchy ∧ x = LIM a`)**: `pos_add`/`pos_mul`: `obtain ⟨a, ⟨ca,hca,ha⟩, hacau, rfl⟩ := hx` (both), `refine ⟨a+b/a*b, ⟨ca+cb/ca*cb, …, fun n => ?_⟩, IsCauchy.add/mul …, ?_⟩`, bound termwise (`linarith`/`mul_le_mul (ha n) (hb n) hcb.le (le_trans hca.le (ha n))`), close LIM with `LIM_add/LIM_mul`. `neg_iff_pos_of_neg`: negate the witness (`-a`, `Pi.neg_apply`, `neg_LIM`). `pos_of_coe` (`(↑q).IsPos ↔ q>0`): forward `rw [ratCast_def, LIM_eq_LIM, equiv_iff]`, `by_contra; push_neg`, instantiate at `c/2`, `abs_le`+`linarith`; backward witness `fun _ => q` with `ratCast_def`. `neg_of_coe` = `rw [neg_iff_pos_of_neg, neg_ratCast, pos_of_coe]`.
- **Chapter 5 ℝ order trichotomy (Section_5_4)**: `trichotomous` (x=0 ∨ IsPos ∨ IsNeg): `by_cases x=0`; else `boundedAwayZero_of_nonzero` gives `a` (Cauchy, `|a n|≥c`, x=LIM a); Cauchy `(IsCauchy.coe _).mp hcau c hc` gives N with `|a j - a k| ≤ c`; `by_cases a N ≥ c` — sign of `a N` propagates (`a n` can't be `≤-c` when `a N≥c` since `|a n-a N|≤c`), so define `a' := fun n => if n<N then c else a n` (BoundedAwayPos), `Equiv a' a` (eventually equal), `isCauchy_of_equiv`, `((LIM_eq_LIM ha'cau hcau).mpr ha'a).symm`. Order lemmas reduce to `IsPos`/`IsNeg` of `x-y` (`gt_iff`/`lt_iff`) + `not_pos_neg`/`not_zero_pos`/`pos_add`; `lt_trans` via `neg_iff_pos_of_neg` + `pos_add` on `-(x-y)`,`-(y-z)`; `antisymm := Iff.rfl`.
- **Chapter 5 ℝ field completion (Section_5_3)**: `Real.zero_ne_one'` via `ratCast_inj`; `exists_pair_ne := ⟨0,1,_⟩`. `self_mul_inv` (x≠0 → x·x⁻¹=1): `obtain ⟨a,hcau,hbaz,rfl⟩ := boundedAwayZero_of_nonzero hx`, `rw [inv_def hbaz hcau, LIM_mul hcau (inv_isCauchy_of_boundedAwayZero hbaz hcau), …ratCast_def]; congr 1; ext n; rw [mul_inv_cancel₀ (nonzero_of_boundedAwayZero hbaz n)]`. `mul_right_cancel₀`: `congrArg (·*z⁻¹) h` + `simp only [mul_assoc, self_mul_inv hz, mul_one]`. `Field.ratCast_def`: `↑q = ↑q.num/↑q.den` via `div_eq_mul_inv` + `inv_ratCast` (forward) + `ratCast_mul` (forward) + `Rat.num_div_den`. `AddGroup.ofLeftAxioms` (add_assoc/zero_add/neg_add_cancel): same `eq_lim`+`LIM_add/neg_LIM`+`congr 1; ext; simp; ring` pattern. `lim_of_boundedAwayZero` (LIM a ≠ 0): `← LIM.zero` + `LIM_eq_LIM` + `equiv_iff` + `push_neg`, witness `c/2`. `IsBounded.equiv` = `(isBounded_of_eventuallyClose (hab 1 _)).mp ha`.
- **Chapter 5 real construction (Section_5_3, `Real := Quotient` of Cauchy seqs)**: `IsCauchy.const` via `IsCauchy.coe` (`dist a a = 0`). `IsCauchy.neg`/`sub`: `dist`-based or `rw [show a-b = a+(-b) by ext n; simp; ring]`. `IsCauchy.mul`: bound `a`,`b` with `isBounded_of_isCauchy` (from 5_1!) → `|a j|≤Ma`/`|b k|≤Mb` (via `hBa ↑j` + `eval_coe_at_int`+`if_pos`+`Int.toNat_natCast`), ε/2 triangle `|a j b j - a k b k| ≤ |a j||b j-b k|+|b k||a j-a k|`, `mul_le_mul` + `mul_div_assoc'`/`div_le_iff₀`+`nlinarith`. `ratCast_add/mul/sub`: `rw [ratCast_def, ratCast_def, ratCast_def, LIM_add/mul]; congr 1` (const-seq sums are defeq, `congr 1` closes). Algebra fields (`add_comm`, `mul_comm`, `mul_assoc`, distrib, `zero_mul`…): `intro …; obtain ⟨a,ha,rfl⟩ := Real.eq_lim x` (per arg), `rw [LIM_add/mul …]`, `congr 1; ext n; simp [Pi.mul_apply, Pi.add_apply]; ring`. `0`/`1` reached via `← Real.LIM.zero` / `show ((1:ℚ):Real) * … ; rw [ratCast_def]`. `ratCast_inj`: `LIM_eq_LIM`+`equiv_iff`, contradiction at `ε=|q-r|/2`.
- **Chapter 5 equivalent sequences (Section_5_2)**: `eventuallyClose_iff` (`EventuallyClose (↑a)(↑b) ↔ ∃N:ℕ ∀n≥N, |a n - b n| ≤ ε`): unfold `eventuallyClose_def`/`closeSeq_def`; coercion `.from N` `.n₀ = max 0 N` (via `Sequence.n0_coe`, `simp [Sequence.from, Sequence.n0_coe]`); `from_eval` + `eval_coe_at_int` + `if_pos` (×2 — one per sequence) + `Int.toNat_natCast`. `equiv_iff` = `rw [equiv_def]; simp_rw [eventuallyClose_iff]`. `isCauchy_of_equiv`/`isBounded_of_eventuallyClose`: prove a one-directional `key` helper, get the `↔` via the symmetric hypothesis (`abs_sub_comm`). Cauchy: ε/3 triangle `|b j - b k| ≤ |b j-a j|+|a j-a k|+|a k-b k|` (`abs_sub_le` twice). Bounded: tail `|b k| ≤ |a k|+ε` + finite head `IsBounded.finite (fun i:Fin N => b i)`, vanish below 0. `Section_4_3.dist_eq : dist x y = |x-y|`; `abs_add` renamed → `abs_add_le`.
- **Chapter 5↔6 ℚ/ℝ bridges** (`isBounded_of_rat`, `IsCauchy_iff`): forward casts ℚ bound/ε up (`exact_mod_cast`); backward needs a rational `≥`/`<` the real (`exists_rat_gt`, `exists_pos_rat_lt` giving `0<q ∧ ↑q<x`). `coe_sequence_eval : (↑a) n = ↑(a n)`. GOTCHA: `simp only [Rat.Close]` (Rat.Close is a `def`) unfolds `a.from N` into its `dite` — finish with `dif_pos hn`/`dif_pos hm` (the condition is `n ≥ max a.n₀ N`, defeq to `(a.from N).n₀`), NOT `from_eval`. The goal `|a n - a m| ≤ ε` with `a n : ℚ`, `ε : ℝ` elaborates LHS as `↑(|a n - a m|)` (ℚ-abs coerced), NOT `|↑(a n) - ↑(a m)|` — match with `((|a n - a m|:ℚ):ℝ)` + `linarith`, not a `(… : ℝ)` ascription.
- **Chapter 6 bounded/Cauchy/convergent** (`bounded_of_convergent`/`bounded_of_cauchy`/`IsCauchy.convergent`): no Chapter-6 finite-bound helper — bound the head `Finset.Icc a.m N` with `Finset.sup' hne (fun n => |a n|)` (`hne := Finset.nonempty_Icc.mpr (le_max_left _ _)`, `Finset.le_sup' f hmem`), tail with `|a n| ≤ |a N|+1` (or `|L|+1`); split `rcases le_or_gt N n` / `le_or_gt a.m n` (use `le_or_gt`, NOT `le_or_lt`), vanish below `a.m`. `IsCauchy.convergent` (convergent⟹Cauchy): `rw [tendsTo_iff]` + `isCauchy_def`, witness `max a.m N0`, `(a.from M).m = M` via `show max a.m (max a.m N0) = …; omega`, `from_eval`, `|a n - a m| ≤ |a n-L|+|a m-L|` at `ε/2`.
- **Chapter 6 `tendsTo_inv`/`div`**: inv via `tendsTo_iff`: get `|a n - L| ≤ |L|/2` (⟹ `|a n| ≥ |L|/2` by `abs_sub_abs_le_abs_sub` + `abs_sub_comm`) and `|a n - L| ≤ ε|L|²/2`; rewrite `(a n)⁻¹ - L⁻¹ = (L - a n)/(a n * L)` (`field_simp`), `abs_div`/`abs_mul`, `div_le_iff₀`, finish with `nlinarith`. `tendsTo_div` reduces to `mul ∘ inv`: prove `a/b = a*b⁻¹` by `ext n <;> rfl` (division is defeq `a * b⁻¹`; the `.m` field `min a.m b.m` matches since `(b⁻¹).m = b.m`), then `rw [key, div_eq_mul_inv]; exact tendsTo_mul ha (tendsTo_inv hb hnon)`. `is_steady_of_rat` (ℚ↔ℝ steady): `rw [Rat.steady_def, Real.steady_def]`, unfold `Rat.Close`(`simp only`, it's a `def`)/`Real.Close`+`Real.dist_eq`+`coe_sequence_eval`, `exact_mod_cast`.
- **Chapter 6 limit laws (`tendsTo_add/sub/smul/mul`)**: all via `rw [tendsTo_iff] at ha hb ⊢; intro ε hε`, split ε (use `ε/2`, or for mul `ε/2/(|M|+1)` & `ε/2/(|L|+1)`), `refine ⟨max Na Nb, fun n hn => ?_⟩`, `rw [Sequence.add_eval/...]`, then `calc |…| = |(a n-L)+(b n-M)| := by congr 1; ring` (ring can't go under `|·|`) → `abs_add_le` → `linarith`. mul also needs `|a n| ≤ |L|+1` (from `ha 1 one_pos`) and `gcongr` + `field_simp` for `(|L|+1)*(ε/2/(|L|+1)) = ε/2`. The `lim_*` corollaries are uniform: `have h := tendsTo_op (lim_def ha) (lim_def hb); refine ⟨⟨_,h⟩, ?_⟩; by_contra hne; exact tendsTo_unique _ hne ⟨lim_def ⟨_,h⟩, h⟩`. `tendsTo_iff` (the ε-N bridge) is the key enabler — see Chapter 6 Sequence entry above.
- **Chapter 6 divergence examples** (`(-1)^n` not steady/Cauchy/convergent, `n+1` unbounded/divergent): coercion `(f:ℕ→ℝ):Sequence` has `.m = 0`; reduce `a (↑k)` via `rw [Sequence.eval_coe]` (NOT `simp only` — sometimes doesn't fire; the `↑k.toNat` path needs `rw [if_pos (by positivity), Int.toNat_natCast]` after the coercion unfolds to a `dite`). For EventuallySteady/Steady use `Real.eventuallySteady_def`/`Real.steady_def`/`Real.close_def`, pick consecutive indices `k, k+1` with `k := (max 0 N).toNat` (`hkz : (↑k:ℤ) = max 0 N` via `Int.toNat_of_nonneg`), `Sequence.from_eval`; `(-1)^k`/`(-1)^(k+1)` differ by 2 via `pow_succ` + `Even/Odd.neg_one_pow` (build `Odd` with `show Odd _ from ⟨k,rfl⟩`, not bare `⟨k,rfl⟩.neg_one_pow`). For ¬Convergent use `tendsTo_iff` at `ε=0.5/1`, even index `2k` (`even_two_mul`) and odd `2k+1`, `abs_le` + `linarith`.
- **Section_7_3 (custom `Series`, ℤ-indexed partial sums)**: `partial s N = ∑ n ∈ Icc s.m N, s.seq n`; the `(a:ℕ→ℝ):Series` coercion has `m=0`, `seq n = if n≥0 then a n.toNat else 0` (`eval_coe a n : (a:Series).seq ↑n = a n`). KEY bridge `partial_coe_eq_range (N) (0≤N) : (a:Series).partial N = ∑ k ∈ range (N.toNat+1), a k` by `induction N, hN using Int.le_induction` — base via `Finset.Icc_self`+`sum_singleton`+`eval_coe`; succ via `partial_succ` (its `N ≥ s.m-1` hyp needs `have hm : (a:Series).m = 0 := rfl; by rw [hm]; omega`) + `sum_range_succ a (N.toNat+1)` made EXPLICIT (else it rewrites the IH's range too). Geometric: `(hasSum_geometric_of_norm_lt_one (by rwa [Real.norm_eq_abs])).tendsto_sum_nat` gives `Tendsto (∑ range M) atTop (𝓝 (1-x)⁻¹)`; shift to `range (M+1)` via `.comp` of a hand-built `Tendsto (·+1) atTop atTop`; bridge ℤ→ℕ with `Tendsto Int.toNat atTop atTop` (`tendsto_atTop_atTop.mpr (fun b => ⟨↑b, fun a ha => by omega⟩)`) + `.congr'` with `filter_upwards [eventually_ge_atTop 0]` + `partial_coe_eq_range`. Diverge: `diverges_of_nodecay` + `Metric.tendsto_atTop` + a large index `max N 0` + `one_le_pow₀`. `absConverges` (abs-series = `mk'` not a coercion): prove `abs.partial = (|x|^·:Series).partial` by `funext N; unfold partial; Finset.sum_congr rfl; by_cases n≥0 <;> simp [Series.abs, Series.mk', Series.instCoe, abs_pow]`, then `refine ⟨_, ?_⟩; unfold convergesTo; rw [hpe]` (NOT `show` — whnf of `.abs.partial` times out).
- **Section_6_epilogue (custom `Chapter6.Sequence` ↔ Mathlib)**: the `(a:ℕ→ℝ):Sequence` coercion has `.m = 0` and `seq n = if n≥0 then a n.toNat else 0`; `eval_coe (n:ℕ) : (a:Sequence) ↑n = a n` (simp). Properties (`IsMonotone`/`BddAbove`/…) are stated `∀ n ≥ a.m, …` over ℤ. Bridge to Mathlib: forward — feed `(k:ℤ)` (membership `by show (0:ℤ) ≤ (k:ℤ); positivity` since `.m=0` is defeq), `rw [show (k:ℤ)+1 = ((k+1:ℕ):ℤ) by push_cast; ring, eval_coe]`; backward — `intro n hn; have hn0 : (0:ℤ) ≤ n := hn; obtain ⟨k, rfl⟩ : ∃ k:ℕ, n=(k:ℤ) := ⟨n.toNat, by omega⟩`. `IsMonotone ↔ Monotone` via `monotone_nat_of_le_succ`/`antitone_nat_of_succ_le`; `BddAbove ↔ BddAbove (range a)` directly (both `∃M, ∀k, a k ≤ M`). `lim = limUnder atTop a`: `(lim_def ha)` + `tendsto_iff_Tendsto` (the file's own bridge to `Filter.Tendsto`) + `.limUnder_eq.symm`.
- **Section_10_2 (extrema/MVT over MATHLIB ℝ)**: `IsMinOn`/`IsMaxOn` via `rw [isMinOn_iff]`/`isMaxOn_iff` (`∀x∈s, f a ≤ f x` / `f x ≤ f a`); refute by feeding one bad point. Fermat: `(h.isLocalMax (Ioo_mem_nhds hx₀.1 hx₀.2)).hasDerivAt_eq_zero (hderiv.hasDerivAt …)` (interior point → `IsLocalMaxOn.isLocalMax` + `HasDerivWithinAt.hasDerivAt`). `IsLocalMaxOn.of_restrict` = `h.filter_mono (nhdsWithin_mono _ hXY)`. Rolle/MVT: `exists_hasDerivAt_eq_zero hab hcont hgab (fun x hx => ((hderiv x hx).differentiableAt (Ioo_mem_nhds hx.1 hx.2)).hasDerivAt)` / `exists_hasDerivAt_eq_slope f (deriv f) hab hcont …`, then `rw [hc0] at hd; exact hd.hasDerivWithinAt`. Refute `IsLocalMax`/`Min` (e.g. x³ at 0): `h.isLocalMax (Icc_mem_nhds …)` then `Metric.eventually_nhds_iff.mp` → `⟨ε,hε,hball⟩`, apply `hball` at `±ε/2` with `dist`-bound proof, `nlinarith [pow_pos …]`. MVT bound: `(convex_Ioo a b).norm_image_sub_le_of_norm_hasDerivWithin_le (fun z hz => (hderiv z hz).hasDerivWithinAt) (fun z hz => by rw [Real.norm_eq_abs]; exact hlip z hz) hx hy` then `abs_sub_comm`. Bounded-deriv→uniform: `differentiableOn_univ`, `lipschitzWith_of_nnnorm_deriv_le hderiv (bound via NNReal.coe_le_coe, coe_nnnorm, Real.coe_toNNReal', le_max_iff)`, `.uniformContinuous.uniformContinuousOn`. NOTE: needs `import Mathlib.Analysis.Calculus.Deriv.Abs` for `not_differentiableAt_abs_zero`. Some examples there (`IsLocalMaxOn x²-x⁴ univ 0`, `IsMaxOn id ℤ n`) are false as stated (degenerate source).
- **Section_10_1 (derivatives over MATHLIB ℝ)**: `HasDerivWithinAt (·^2) (2x₀) univ x₀`: `(hasDerivAt_pow 2 x₀).hasDerivWithinAt` then `convert … using 1; norm_num` (coefficient is `↑2 * x₀^(2-1)`). Difference-quotient limits of `abs` at 0: `tendsto_nhds_of_eventually_eq` + `filter_upwards [self_mem_nhdsWithin]`, rewrite `abs_of_pos`/`abs_of_neg`/`abs_zero`, finish `div_self (sub_ne_zero.mpr hx.ne')` (right) / `sub_zero, sub_zero, neg_div, div_self (ne_of_lt hx)` (left). One-sided differentiability of `abs`: build `HasDerivWithinAt abs 1 (Ioi 0) 0` via `(hasDerivWithinAt_id 0 _).congr (fun y hy => abs_of_pos hy) abs_zero`, and `(-1)` on `Iio 0` via `((hasDerivWithinAt_id ..).neg).congr (… abs_of_neg ..) (by simp)`; then `.differentiableWithinAt` / `.derivWithin (uniqueDiffWithinAt_Ioi/Iio 0)`. Non-differentiability counterexample: `not_differentiableAt_abs_zero h.differentiableAt` after `rw [hasDerivWithinAt_univ]`.
- **Section_9_9 (ε-δ / uniform continuity over ℝ)**: `extract_lets f ε x₀ δ` then `simp only [f, ε, x₀, δ] at *` to unfold the `let`s; bound `|1/x - 1/x₀|` via `rw [abs_le] at h ⊢`, derive `0 < x` by `linarith`, and bound `1/x` with `le_div_iff₀`/`div_le_iff₀ hxpos` + `nlinarith`. Unboundedness of `1/x`: witness `1/(|M|+1) ∈ Icc 0 2` then `one_div_one_div` + `le_abs_self`.
- **Section_9_8 (monotonicity over MATHLIB ℝ)**: `StrictMonoOn`/`StrictAntiOn` of `x^2` by `intro a ha b hb hab; simp only [Set.mem_Ici/Iic] at *; nlinarith`. `¬MonotoneOn`/`¬AntitoneOn` by feeding two points: `h (Set.mem_univ p) (Set.mem_univ q) (by norm_num)` then `norm_num at this`. `StrictMonoOn.monotoneOn`/`StrictAntiOn.antitoneOn`; `monotoneOn_const`/`antitoneOn_const`. `¬StrictMonoOn const` (Nontrivial X): `obtain ⟨x,y,hxy⟩ := exists_pair_ne X; rcases lt_trichotomy x.val y.val` (use `x.2`/`Subtype.ext`). `IsMaxOn` of monotone on `Icc a b`: witness `b`, `rw [isMaxOn_iff]`, finish with `hf hx (mem b) (mem_Icc.mp hx).2`; antitone → witness `a`. `BddOn f (Icc a b)` (`∃M,∀x∈X,|f x|≤M`) of monotone: `⟨|f a|+|f b|, …⟩`, derive `f a ≤ f x ≤ f b`, `rw [abs_le]; constructor <;> nlinarith [neg_abs_le, le_abs_self, abs_nonneg …]`. Monotone-but-discontinuous witness: `if x≥0 then 1 else 0` on univ (monotone by `by_cases a≥0` + `simp`; not-continuous via `continuousOn_univ` then the `tendsto_nhds_unique` left-limit trick). NOTE: the `no_*_intermediate_value` statements have a duplicated `∨` making them require an empty `Icc (f a) (f b)` — unprovable for strict-mono (degenerate source).
- **Section_9_4 (continuity over MATHLIB ℝ)**: const/id → `continuousWithinAt_const`/`continuousAt_const`/`continuousOn_const`/`continuous_const`/`continuous_id`; `continuous_abs`; polynomials/sums → `continuous_finset_sum` + `fun_prop`; `ContinuousOn.restrict` = `hf.mono hY`. rpow: `Real.continuous_const_rpow (h:a≠0)` for `a^x`; `Real.continuousAt_rpow_const x p (Or.inl/inr …)` for `x^p` (turn into `ContinuousOn` by `intro x hx; exact (…).continuousWithinAt`); `Continuous.rpow_const (hf) (fun x => Or.inr (Real.sqrt_nonneg _))` for `|…|^√2`; `Continuous.div` with the `∀x, g x ≠ 0` side goal by `intro x; positivity`. Locally-constant functions (`Real.sign`, step `if x≥0`): away from the break, `(continuousAt_const (y:=…)).congr (by filter_upwards [Iio_mem_nhds hx / Ioi_mem_nhds hx] with y hy using …)`; at the break, refute continuity with `tendsto_nhds_unique h1 h2` where `h1 := hcont.continuousWithinAt.tendsto` (on `nhdsWithin 0 (Ioi/Iio 0)`, NeBot auto) and `h2` from `tendsto_nhds_of_eventually_eq` + `self_mem_nhdsWithin`. `ContinuousWithinAt f s x` unfolds (via `show`) to `Tendsto f (nhdsWithin x s) (nhds (f x))`.
- **Section_4_3 (abs/exponentiation over MATHLIB `ℚ`)**: nearly all are direct Mathlib lemmas (the file re-states them in namespace `Section_4_3`, so use `_root_.` to reach Mathlib and dodge the self-recursion). Renames to know: `abs_add` → `_root_.abs_add_le`; base-monotone pow → `pow_le_pow_left₀`/`pow_lt_pow_left₀` (the `₀` variants); `abs_pow`/`abs_zpow`/`zpow_add₀`/`mul_zpow` exist; `abs_le.symm` for `-y≤x∧x≤y ↔ |x|≤y`; `le_abs := ⟨neg_abs_le, le_abs_self⟩`; `abs_sub_le` for triangle, `abs_sub_comm`. `Nat.lt_two_pow_self` for `N < 2^N`. GOTCHA: this file defines a LOCAL `zpow_neg (x:ℚ)(n:ℕ) : x^(-↑n)=1/x^n` that shadows Mathlib's — use `_root_.zpow_neg` for the general `a^(-n)=(a^n)⁻¹`. `Close` lemmas: `rw [close_iff] at *` (unfolds to `|x-y|≤ε`) then `abs_le`/`linarith`/`calc … abs_add_le`/`abs_sub_le`. zpow base-injectivity (`zpow_inj`): `rcases lt_or_gt_of_ne hn`; for n<0 reduce via `_root_.zpow_neg` to `-n>0`; then `(zpow_left_strictMonoOn₀ hn').injOn (le_of_lt hx) (le_of_lt hy) hxy`. n<0 antitone: `inv_anti₀ hyp h1` after rewriting `x^n = (x^(-n))⁻¹` by `simp`.
- **Chapter 4 rationals (`Section_4_2.Rat`, quotient of `a // b` with `b≠0`)**: destructure `obtain ⟨a,b,hb,rfl⟩ := eq_diff x` (carries the `b≠0` proof). `Rat.eq a c hb hd : a//b = c//d ↔ a*d = c*b` (needs BOTH denom≠0 proofs). `add_eq`/`mul_eq`/`neg_eq`/`inv_eq` similarly take denom-nonzero args. Ring fields: obtain all, `rw [add_eq/mul_eq …, eq _ _ <Int.mul_ne_zero…> <…>]; ring` — supply each composite denominator's nonzero proof via `Int.mul_ne_zero`/`one_ne_zero`. Setoid `trans`: `linear_combination f*h1 + b*h2` then `mul_right_cancel₀ hd`. Well-defined lifts: `simp_all [Quotient.eq]` often closes, else `linear_combination`. `inv` lift: `by_cases a=0` (junk-value branch closed by `simp [formalDiv, …]`; nonzero branch by `Rat.eq` + `linear_combination`). `decidableEq`: `apply decidable_of_iff (a*d=c*b); constructor <;> [Quotient.sound; Quotient.exact]` (≈ is defeq to the ℤ equation). `Field.mul_inv_cancel`: derive numerator≠0, then `inv_eq, mul_eq, eq; ring`. intCast/RingHom maps: `(intCast_add a b).symm` etc. Cast injectivity needs a beta-reducing `have h2 : (↑a:Rat) = ↑b := h` before rewriting. Bare `example`s outside the `Rat` namespace need `Rat.` prefixes (e.g. `Rat.div_eq`).
- **Chapter 4 integers (`Section_4_1.Int`, quotient of formal diffs `a —— b`)**: destructure any `x:Int` with `obtain ⟨a,b,rfl⟩ := eq_diff x`. Key rewrites: `add_eq`/`mul_eq`/`neg_eq` (compute on representatives, defeq via `Quotient.lift_mk`), `Int.eq` (`a——b = c——d ↔ a+d = c+b`), `natCast_eq` (`↑n = n——0`), and `(0:Int) = 0——0` / `(1:Int) = 1——0` / `(-1:Int) = 0——1` by `rfl`. Ring-instance fields: `obtain` all vars, `simp_rw [add_eq, mul_eq]; congr 1 <;> ring`; `AddGroup.ofLeftAxioms` takes (assoc)(zero_add)(neg_add). Order: `le_iff` (`a≤b ↔ ∃t:ℕ, b=a+↑t`), `lt_iff`, `lt_iff_exists_positive_difference`. To kill `a = a + ↑x` use `add_left_cancel` on `a+0 = a+↑x` (NOT `self_eq_add_right` — doesn't exist); when `a` appears on BOTH sides of a rw target, use `push_cast; linear_combination …` instead of `rw`. Cross-type goals (ℕ casts in ℤ) close with `omega`/`push_cast; omega`/`linear_combination`. No-zero-divisors: expand `(a——b)(c——d)`, get `((p:ℤ)-q)*((r:ℤ)-s)=0`, apply `_root_.mul_eq_zero`. Build `≃+*o` to Mathlib `ℤ` via `Quotient.lift (fun ⟨a,b⟩ => (a:ℤ)-b)`, inverse `z ↦ z.toNat —— (-z).toNat`; `left_inv`/`right_inv`/`map_*` all close by `show <reduced ℤ form>` + `omega`/`push_cast; ring`. CAUTION: a lemma can only transport through the iso if it's stated AFTER the iso is defined — early lemmas (mul_eq_zero, cancel, mul_lt) must be proved directly.
- **Chapter 2 Peano (`Chapter2.Nat`, inductive zero/succ, `n++` = succ)**: `Nat.induction (motive) (base:P 0) (step:∀n, P n→P(n++)) : ∀n, P n` is the recursor — use `refine Nat.induction (fun n => …) ?_ ?_ x` (NOT Mathlib `induction`); beta-reduce motive goals with `show` before `rw`. Core rewrites: `succ_add` (`n++ + m = (n+m)++`, rfl), `add_succ`, `zero_add`/`add_zero`, `one_add` (`1+m = m++`). `1 = 0++` by rfl. Order: `Nat.le_iff (n m) : n≤m ↔ ∃a, m=n+a` (TAKES explicit args — `(le_iff m N).mpr`, not `le_iff.mpr`); `lt_iff`, `lt_iff_succ_le` (`a<b ↔ a++≤b`), `le_iff_lt_or_eq`. CAUTION: Mathlib `add_right_cancel` needs `IsRightCancelAdd` (absent) — use the local `Nat.add_left_cancel a b c : a+b=a+c → b=c` (after `add_comm`). `abel` works once `AddCommMonoid` instance is in scope (defined mid-file). `rcases d with _ | d'` gives the `zero` constructor (NOT syntactic `0`) — `rw [add_zero]` won't match `a + zero`; use defeq via `hd.trans (add_zero a)` or `simp`. Predecessor of a positive: `rcases a with _ | b` then `absurd rfl ha`. Strong/backwards/from-point induction: build a `key : ∀ N, …` by `Nat.induction` on the bound, then instantiate (e.g. `key (m++) m hm (succ_gt_self m)`).
- **Chapter 2 epilogue (Chapter2.Nat ↔ ℕ)**: `toNat` conversion; `zero_toNat`/`succ_toNat` (`(n++).toNat = n.toNat+1`). Preserve `+`/`*`/`^` by `induction' n with n hn` then `rw [succ_add/succ_mul/pow_succ, succ_toNat, hn, …]` and finish `omega`/`ring` (qualify Mathlib lemmas with `_root_.`, e.g. `_root_.Nat.zero_add`, `_root_.pow_succ`). Order: `Nat.le_iff : n≤m ↔ ∃a, m=n+a` vs Mathlib `le_iff_exists_add`; bridge via `equivNat.injective` and `equivNat.right_inv : (↑k:Chapter2.Nat).toNat = k`.
- **Abstract Peano (`PeanoAxioms`)**: `natCast P : ℕ → P.Nat` (recursive). Injective: auxiliary `∀ n m, natCast n = natCast m → n = m` by `induction' n` + `cases m` (cross cases via `P.succ_ne`, succ case via `P.succ_cancel` + `congrArg`). Surjective: `refine P.induction (fun y => ∃ n, natCast n = y) ⟨0, rfl⟩ ?_ y`. Build `P.Nat ≃ ℕ` with `Equiv.ofBijective P.natCast ⟨inj, surj⟩` (NOT `Function.invFun` — needs `Nonempty`; use `Function.surjInv`/`leftInverse_surjInv`/`rightInverse_surjInv` if hand-rolling). `natCast 0 = P.zero` and `natCast (m+1) = P.succ (natCast m)` hold by `rfl`. Transport recursion (`recurse_uniq`): define `g : ℕ → P.Nat` by `Nat.rec`, set `a x := g (ic.symm x)`; key `ic.symm (P.succ x) = ic.symm x + 1` via `ic.injective` + `apply_symm_apply` + the succ-rfl. `Equiv` (class) `equiv_zero`/`equiv_succ` for symm/trans: `apply equiv.injective; simp only [equiv_succ, Equiv.apply_symm_apply]` / `simp only [Equiv.trans_apply, …]`. P.induction goals need `show` to beta-reduce the motive before `rw`.
- **Cardinality (Chapter 8)**: `EqualCard X Y := ∃ f, Bijective f`; `LeCard X Y := ∃ f, Injective f`; `LtCard X Y := LeCard X Y ∧ ¬ EqualCard X Y`. Schröder–Bernstein = `Function.Embedding.schroeder_bernstein (hf:Injective f) (hg:Injective g) : ∃ h, Bijective h` — exactly `LeCard X Y → LeCard Y X → EqualCard X Y`. From `EqualCard X Y` (a bijection `e`) get the reverse injection via `(Equiv.ofBijective e he).symm` + `.injective`. For `LtCard` transitivity/antisymmetry, reduce `¬EqualCard`/`¬LeCard` goals to a contradiction by building both injections and applying Schröder–Bernstein. `Preorder.lt_iff_le_not_ge` field goal is `a<b ↔ a≤b ∧ ¬b≤a`.
- **Chapter 3 SetTheory.Set algebra**: `apply ext; intro x` reduces set equality to membership iff (`SetTheory.Set.ext` from `∀x, x∈X ↔ x∈Y`). Membership lemmas all take explicit Object/Set args: `mem_inter x A B`, `mem_union x A B`, `mem_sdiff` (`x∈A\B ↔ x∈A ∧ x∉B`), `mem_singleton x a`, `mem_pair x a b`. For partition/union-decomposition identities, `simp only [mem_union, mem_inter, mem_sdiff]` then `by_cases hA : x∈A <;> by_cases hB : x∈B <;> tauto`. `eq_empty_iff_forall_notMem` + `not_mem_empty` for emptiness contradictions. Coercion `(X:Object)` is injective: `coe_eq_iff`/`coe_eq` (and `Subtype.coe_injective` for element subtypes). `{a,b}={c,d}` decomposition (`pair_eq_pair`): extract the 4 membership facts via `mem_pair` and finish with `grind` (handles the transitivity/case-split). `Pairwise (onFun Disjoint ![...])`: `intro i j hij; rw [Function.onFun, disjoint_iff, eq_empty_iff_forall_notMem]; intro x hx; fin_cases i <;> fin_cases j <;> simp_all [...]`.
- **Bridging custom `Set` ↔ Mathlib `_root_.Set Object`**: `coe_inj'` (@[simp]), `coe_subset`, `coe_intersection`/`coe_empty`/`coe_union` rewrite between the two. For `coe_ssubset`: `rw [ssubset_iff_subset_ne (Mathlib side), SetTheory.Set.ssubset_def (custom side), coe_subset]; simp [coe_inj']` — the custom `⊂` has NO Preorder instance so `ssubset_iff_subset_ne` only fires on the Mathlib coe side; use the custom `ssubset_def` (`X⊂Y ↔ X⊆Y ∧ X≠Y`) for the other. For `coe_Disjoint`: `rw [Set.disjoint_iff_inter_eq_empty, ← coe_intersection, ← coe_empty, coe_inj', SetTheory.Set.disjoint_iff]` (qualify `disjoint_iff` — it's ambiguous with Mathlib's).
- **`singleton_iff` pattern** ("no proper nonempty subset ↔ singleton"): forward — take `x` from `nonempty_def`, show `A={x}` by `ext`; for `y∈A` use `by_contra`, then exhibit `{x} ⊂ A` (proper since `y∈A, y∉{x}`) and nonempty to contradict the hypothesis. Backward — from `B ⊂ {x}` and `B≠∅`, pull `b∈B`, show `B={x}` (every element forced to `x` via `hsub`+`mem_singleton`, and `x=b∈B`), contradicting strictness.
- **Specification from replacement**: to get `∃B⊆A, ∀x, x.val∈B ↔ P x` without `specify`, use `A.replace (P := fun (a:A) (y:Object) => y = a.val ∧ P a)` with functionality `hP` proved by `hy.trans hy'.symm`; membership via `replacement_axiom`, and collapse `a.val=x.val → a=x` with `Subtype.coe_injective`.
- **Chapter 3 SetTheory.Set existence results**: when given `h : axiom_of_universal_specification` (= `∀ P, ∃ A, ∀ x, x∈A ↔ P x`), every "∃ set with membership ↔ φ" exercise is just `obtain ⟨X, hX⟩ := h (fun z => φ z); exact ⟨X, hX⟩` (emptyset/singleton/pair/union/specify/replace all this shape; for emptyset use `fun _ => False` and `(hX x).mp hx`). `univ_iff` backward: build `U.specify (fun u => P u.val)` and finish with `specification_axiom' (A := U) _ ⟨x, hU x⟩` + `simpa`. `no_univ := by rw [← univ_iff]; exact Russells_paradox`.
- **Regularity (`not_mem_self`/`not_mem_mem`)**: apply `axiom_of_regularity (nonempty_of_inhabited hmem)` to a singleton `{(A:Object)}` / pair `{A,B}`; `axiom_of_regularity` yields `⟨x, hx⟩` with `hx : ∀ S:Set, x.val = ↑S → Disjoint S _`. Identify `x.val` via `(mem_singleton _ _).mp x.property` / `(mem_pair _ _ _).mp x.property`, get `Disjoint`, then `rw [disjoint_iff, eq_empty_iff_forall_notMem]` and feed `(mem_inter _ _ _).mpr ⟨_, _⟩` to derive the contradiction. `mem_inter`/`mem_singleton`/`mem_pair` all take explicit Object args.
- **Decidable counterexamples over custom `Function` (Chapter 3)**: `apply isFalse; intro H` then apply `H` to concrete witnesses. Build functions with `mk_fn (fun x => …)`; evaluate with `eval_of` (`(mk_fn f) x = f x`) and `comp_eval` (`(g○f) x = g (f x)`). Use `Nat` as the generic infinite set; `(0:Nat)`/`(1:Nat)` are distinct by `simp` (via `ofNat_inj`). For "g∘f injective but g not": pick domain `X = ({(0:Object)}:Set)` (a singleton) so injectivity is vacuous — any two elements equal via `Subtype.ext` of `(mem_singleton _ _).mp x.property`. For "g∘f onto but f not": pick codomain `Z = ({(0:Object)}:Set)` so `onto` is trivial. NOTE: `g (f x)` where `g = mk_fn (fun _ => c)` reduces with a SINGLE `eval_of` (the inner `f x` is discarded) — don't double-rewrite. `mk_fn`/`mem_singleton` need the `SetTheory.Set.` prefix in the `Chapter3` namespace.
- **Gluing functions on a union (`Function.glue`/`glue'`)**: build the Mathlib function `hfun : (X∪Y) → Z := fun w => if hw : w.val ∈ X then f ⟨w.val,hw⟩ else g ⟨w.val, (hmemXY w).resolve_left hw⟩` (with `classical`), then `refine ⟨mk_fn hfun, ⟨?_,?_⟩, ?_⟩`. Key lemma to prove once: `hincl_val : (inclusion h a).val = a.val` by `simp [Function.inclusion, eval_of]`. CRITICAL: `(inclusion h a).val` is NOT defeq to `a.val` (to_fn uses choice), so you can't `exact`/`rw` through it — and `rw [hincl_val …]` on the if-SCRUTINEE fails with "motive is not type correct". Instead: after `comp_eval, eval_of`, set `hv := hincl_val …`, then discharge the `dif_pos`/`dif_neg` condition with `(by rw [hv]; exact …)` and fix the function argument with `congr 1; apply Subtype.ext; exact hv`. To swap an argument before applying a hypothesis (e.g. `hfg` on the intersection), `rw [show (⟨(inclusion ..).val, h⟩ : X) = ⟨y.val, h'⟩ from Subtype.ext hv]`. Uniqueness: `by_cases hwX : w.val ∈ X` (NOT `rcases hmemXY` — the Y-case mustn't assume ∉X), build `inclusion .. ⟨w.val,_⟩ = w` via `Subtype.ext (by rw [hincl_val ..])`, then `calc` through `comp_eval` + the hypothesis.
- **Custom `Function` (Chapter 3, SetTheory.Set)**: `Function.eq_iff f g : f = g ↔ ∀ x, f x = g x` (use `rw [eq_iff]; intro x`). `comp_eval` unfolds `(g ○ f) x = g (f x)`. `inverse_eval h y x : x = (f.inverse h) y ↔ f x = y`. Inverse facts: `inverse_comp_self h x : (f.inverse h)(f x)=x`, `self_comp_inverse h y : f ((f.inverse h) y)=y`. one_to_one via `rw [one_to_one_iff]` then push hyp through `f` with `by rw [hyp]` + `rwa [self_comp_inverse, self_comp_inverse]`.
- Defeq `if h:P then ... else ...` with a known proof: `rw [dif_pos hex]` substitutes your `hex` for the chooser; then `congr 1` + `Nat.cast_injective hex.choose_spec` to pin the witness.

## Conventions
- Proofs intentionally follow the textbook structure (comments say so); prefer faithful over golfed.
- Chapter namespaces: `Chapter9`, `Chapter10`, etc. Custom `Nat` lives in Section 2.
